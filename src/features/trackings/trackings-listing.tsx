import TrackingsListingTable from './trackings-listing-table';

type TrackingListingPage = {};

export default async function TrackingListingPage({}: TrackingListingPage) {
  return (
    <TrackingsListingTable />
  );
}
